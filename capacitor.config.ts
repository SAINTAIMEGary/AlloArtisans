import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'AlloArtisans',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
